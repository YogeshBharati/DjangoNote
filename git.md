### How to Push Code From Server on GitHub
```
ls
git status
git add .
git config --global user.email "your_email"
git commit -m "comment"
git push origin master
```
### How to pull code from git on local computer
### On vs code
```
git pull origin master
```
### Edit your codes on vs code
```
git add .
git commit -m "comment"
git push origin master
```
### How to pull edited code from git on Server
### 1: login your server with sshkey
#### 2: go to your project
```
git pull origin master
```
### Enter the passwoed if asked.