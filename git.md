### How to Push Code From Server on GitHub
### Step:1 login the server and cd you_project
```
ls
git status
git add .
git config --global user.email "your_email"
git commit -m "comment"
git push origin master
```